{
  "name": "antigravity-proxy-stack",
  "version": "1.0.0",
  "description": "Cloudflare Workers API Proxy",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}